{
  "name": "noesis-figma-plugin",
  "version": "1.0.0",
  "description": "Figma plugin to export designs as NoesisGUI XAML",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.101.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
